<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
# Agent Operating Guidelines (AI行動規範・制御ルール) - Typing Universe

本プロジェクト（`typing-universe`）は、**「タイピングゲームを中心とした複数ゲームの切り替え可能なプラットフォーム」** を構築するプロジェクトです。
**Feature-Sliced Design (FSD)** および **ドメイン駆動設計 (DDD)** の思想をベースに、厳格なテストとコードレビューによって品質を担保しつつ、**できるだけ自律的に（自動で）開発を進行**させることを目指します。

---

## 1. 最重要ルール：自律進行とコミット前の絶対確認

1. **ファイル編集の自動進行**:
   - 新規機能の実装やバグの修正において、**ファイルの作成・編集・テスト実行については、事前の許可を求めずに自動で進めて構いません**（事後報告でOK）。
   - マイクロマネジメントを避けるため、ある程度まとまった単位で実装を自動完結させてください。
2. **コミット＆プッシュ前の絶対確認 (CRITICAL)**:
   - **コードを書き終え、テストを通過した後は、絶対に勝手に `git commit` や `git push` を行ってはいけません。**
   - 必ず「このような修正を行いました。これでコミットしてよろしいでしょうか？」とユーザーに提案し、**明確な許可を得てから**コミットしてください。
3. **絵文字の禁止と日本語対応**:
   - 解説、ドキュメント、コミットメッセージ、質疑応答はすべて**自然な日本語**で行ってください。
   - **絵文字は一切使用しないでください**（コード、コメント、コミットメッセージ、ドキュメントすべてにおいて禁止）。

---

## 2. アーキテクチャと設計方針

本プロジェクトは **Feature-Sliced Design (FSD)** を採用します。機能（Feature）ごとに垂直にスライスし、高い凝集度と低い結合度を目指します。

1. **ドメイン駆動設計 (DDD) の適用**:
   - FSDの各スライス内に純粋なドメインモデル（Entity, Value Object）を配置します。
   - 外部依存（UI、API、状態管理ライブラリなど）をドメイン層に持ち込まない純粋性を維持します。
2. **境界づけられたコンテキスト（Bounded Context）**:
   - 複数ゲームが存在するため、プラットフォーム共通の `core` ドメインと、各ゲーム固有のドメイン（`invader`等）を明確に分割し、ゲーム間の越境アクセスを禁止します。
3. **技術スタックと外部ツール連携**:
   - インフラ：基本はVercelへのデプロイ（Supabase等のRDBは使用しない）。
   - BaaS：ランキングや現在プレイ人数のリアルタイム同期など、軽量なバックエンド機能が必要な場合は **Firebase (Realtime Database / Firestore)** の使用を許可する。
   - 認証：現状はログイン機能なし。
   - SEO対策：適切なメタタグ、OGP設定などを実装する。
   - UIデザイン生成：**StitchMCP** を使用する場合は、常に共通プロジェクト (ID: `15453124055556575502` / [URL](https://stitch.withgoogle.com/projects/15453124055556575502?pli=1)) を指定し、デザインの一貫性を保つこと。

---

## 3. テスト設計戦略と役割分担

本プロジェクトは複数のゲームを統合するプラットフォームであるため、以下の明確なテストの役割分担を定義します。

1. **Vitest（ドメイン単体テスト）の役割 ＝ 「ゲームの頭脳・ロジックの証明」**
   - ゲームのコアルール、タイピング判定、スコア計算、時間経過（Tick）などの「数学的・論理的なロジック」をカバレッジ100%で検証します。
   - 実際の時間に依存せず、任意の `deltaTime` を注入して境界値や異常系を網羅するテスト（Tickテスト）を実装します。
   - 単体テストではUIや外部APIのモックを最小限にし、純粋なTypeScriptクラスの入出力をミリ秒単位で検証します。
2. **Playwright（E2Eテスト）の役割 ＝ 「プラットフォームの枠組み・DOMの検証」**
   - Canvas内部のゲームロジックには関与せず、ゲーム選択画面、サウンド設定等の状態保存、ゲームオーバー後のリザルト画面など、HTML/DOMベースのプラットフォーム共通機能の結合テストを担当します。
3. **人間（目視）の役割 ＝ 「ゲームの体験・Canvasビジュアルの確認」**
   - Canvasに描画されるエフェクトの美しさ、ズレ、ゲームバランスや爽快感は人間が実際にプレイして評価します。
4. **AAAパターンの徹底**
   - 全てのテストケースは Arrange（準備） / Act（実行） / Assert（検証） の3ステップで記述します。

---

## 4. AIと人間の協調ワークフロー（レビューと品質担保）

1. **実装の自動完結**:
   - 指示を受けたら、関連するFSDのスライスを作成し、UIコンポーネントからドメインロジック、テストまで一気に自動で書き上げてください。
2. **自動厳格コードレビュー (Strict Code Review)**:
   - まとまった実装が完了した際は、AI自身（またはサブエージェント）によって客観的なレビューを行い、以下の項目を検証してください。
     - FSDの依存方向ルールが守られているか。
     - ドメインの純粋性が保たれているか。
     - テストカバレッジと境界値テストが十分か。
3. **最終報告**:
   - 実装とテスト、静的解析が完了した時点で、ユーザーに「実装内容のサマリとテスト結果（およびレビュー結果）」を報告し、コミットの許可を求めます。

## AI自己診断と厳格コードレビュー (Strict Code Review) の強制
- **ルール**: メインAI（あなた）は、機能実装やリファクタリングを終え、ユーザーにコミットの許可を求める「前」に、必ず自主的に `invoke_subagent` を使って `strict-code-review` スキルを持つサブエージェントを起動してください。
- **ワークフロー**:
  1. `npm run typecheck` と `npm run test` をパスする。
  2. サブエージェント (`invoke_subagent` ツール) を起動し、現在の実装のレビューを依頼する。
  3. レビュー結果を受け取り、指摘があれば修正する。
  4. 全てクリアになった状態でのみ、ユーザーに「コミットしてもよろしいでしょうか？」と許可を仰ぐ。
- **目的**: ユーザーが手動でレビューを指示する手間を省き、AI同士の相互監視による品質担保プロセスを完全自動化するため。

## 5. ai-note-market由来の高度な設計思想の適用
本プロジェクトでは、パフォーマンスと保守性を極限まで高めるため、以下の設計思想を必須とします。

1. **Hooksの絶対分離 (UIの純粋性)**:
   `.tsx` ファイル内には `useState` や `useEffect` などの状態管理・副作用ロジックを直書きしません。すべてのロジックは必ず Custom Hook (`useXxx.ts`) に隔離し、UIは状態の表示に徹します。
2. **Flyweight (キャッシュ) パターンの適用**:
   タイピングゲーム特有の60FPSでの高頻度更新に耐えるため、単語やキーの Value Object など、不変で頻出するインスタンスは毎回 `new` せず、静的ファクトリ（`create`等）内でキャッシュ・再利用し、GCの発生を抑制します。
3. **Zodによる境界防衛**:
   ドメインモデルに渡す前のキーボード入力などの検証は、UIやHooks層の手前で Zod 等を用いてサニタイズし、ドメインの安全性を保証します。
4. **テスト名の日本語仕様書化**:
   テストコードの `it(...)` は、「〜の場合、〜になる」という日本語で記述し、テスト結果自体が仕様書として機能するようにします。
5. **変更履歴・改修コメントの完全禁止 (クリーンコードの維持)**:
   コードを改修する際、コード内に `// 修正:` や `// 〜から〜へ変更` などの変更履歴・差分理由や、AI特有の長文の言い訳・解説コメントを絶対に記述してはなりません。変更理由はすべて Git コミットメッセージとPRで表現し、コード内には「現在の仕様（JSDoc規約に沿った単位・境界制約・設計意図）」のみを残してください。
