name: Sync NotebookLM Google Docs

on:
  push:
    branches:
      - main
    paths:
      - "docs/notebooklm/**"
      - "scripts/sync-google-docs.mjs"
      - ".github/workflows/sync-google-docs.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Sync Markdown sources to Google Docs
        env:
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
          NOTEBOOKLM_PRODUCT_DOC_ID: ${{ secrets.NOTEBOOKLM_PRODUCT_DOC_ID }}
          NOTEBOOKLM_ENGINEERING_DOC_ID: ${{ secrets.NOTEBOOKLM_ENGINEERING_DOC_ID }}
        run: node scripts/sync-google-docs.mjs
